# 看房助手开发计划书 (Property Viewing Assistant Development Plan)

本文档基于当前已完成的高保真交互原型，详细梳理了前端录入、列表查看及后台管理三大核心模块的功能元素、交互逻辑及后续开发计划。

---

## 1. 核心模块一：移动端 - 极速录入 (Mobile Entry)

### 1.1 功能元素清单
*   **位置信息 (Location)**
    *   **行政区 (Administrative District)**: 下拉选择（上海全量行政区）。
    *   **板块 (Plate)**: 根据行政区联动显示，支持手动输入补充。
    *   **详细地址 (Address)**: 多行文本框，支持输入小区名及门牌号。
*   **房源详情 (Property Details)**
    *   **面积 (Area)**: 数值输入 (㎡)。
    *   **楼层 (Floor)**: 文本输入 (如 "5/20")。
    *   **房龄 (Age)**: 年份输入 (4位数字)，包含有效性校验 (1900-至今)。
    *   **装修 (Decoration)**: 单选按钮 (毛坯/简装/精装/豪装)。
    *   **照片 (Photos)**: 支持拍照或从相册上传，支持预览及删除。
*   **价格分析 (Price Analysis)**
    *   **总价 (Total Price)**: 数值输入 (万元)。
    *   **参考单价 (Unit Price)**: **自动计算** (总价/面积)，单位 元/㎡。
    *   **预计租金 (Estimated Rent)**: 数值输入 (元/月)。
    *   **租售比 (Rental Yield)**: **自动计算** (年租金/总价)，单位 %。
*   **周边配套 (Surrounding Facilities)**
    *   **类型**: 地铁、医疗、学区、商圈。
    *   **交互**: 点击“添加”按钮动态增加条目。
    *   **字段**: 名称 (必填)、距离 (可选，单位米)。
*   **操作栏 (Actions)**
    *   **保存按钮**: 底部悬浮，点击提交数据。

### 1.2 逻辑与规则
1.  **自动计算**: 输入总价/面积/租金时，实时更新单价和租售比。
2.  **重复录入校验**: 提交前检查“行政区+地址+楼层”是否重复，重复则拦截并提示。
3.  **防抖与状态**: 提交过程中按钮置灰并显示 loading，防止多次点击。
4.  **数据持久化**: 通过 Supabase Client 写入 `properties` 及 `property_facilities` 表。

### 1.3 开发计划
- [x] **UI/UX 实现**: 完成响应式布局与交互开发。
- [x] **数据绑定**: 完成 Zustand Store 与表单的双向绑定。
- [x] **后端对接**: 完成 Supabase 写入接口对接。
- [x] **逻辑校验**: 完成房龄校验、重复录入校验。
- [ ] **图片存储优化**: 目前图片可能以 Base64 形式处理，后续需接入 Supabase Storage 实现云端文件存储，优化数据库性能。
- [ ] **板块数据补全**: 完善上海所有行政区的板块字典数据。

---

## 2. 核心模块二：移动端 - 房源列表 (Mobile List)

### 2.1 功能元素清单
*   **列表容器**: 垂直滚动列表。
*   **房源卡片**:
    *   **封面图**: 展示录入的第一张照片，若无照片显示占位符。
    *   **核心信息**: 地址、区域/板块、楼层、房龄。
    *   **辅助信息**: 装修情况、面积。
    *   **配套标签**: 展示前3个录入的配套名称（如“2号线”、“仁济医院”）。
    *   **价格展示**: 红色高亮显示总价，灰色显示单价。
*   **操作**:
    *   **删除**: 卡片右上角垃圾桶图标，点击需二次确认。

### 2.2 逻辑与规则
1.  **实时同步**: 页面加载时自动从云端拉取最新数据。
2.  **排序规则**: 默认按录入时间倒序排列（最新的在最前）。
3.  **状态反馈**: 加载中显示 Loading 占位，无数据显示空状态插画。

### 2.3 开发计划
- [x] **UI 实现**: 完成卡片式布局开发。
- [x] **数据对接**: 完成 Supabase 查询与删除接口对接。
- [ ] **分页加载**: 当数据量超过 20 条时，实现下拉加载更多 (Infinite Scroll)。
- [ ] **图片懒加载**: 优化列表页图片加载策略，提升滚动流畅度。

---

## 3. 核心模块三：Web端 - 决策分析台 (Admin Dashboard)

### 3.1 功能元素清单
*   **数据概览 (Dashboard)**
    *   **统计卡片**: 已看房源总数、平均总价、平均单价。
*   **房源明细表 (Data Table)**
    *   **表头**: 区域/板块、小区/地址、总价、单价、面积、房龄、配套详情、录入时间。
    *   **内容**: 对应数据库的全量数据展示。
*   **工具栏**:
    *   **搜索框**: 输入小区名称进行过滤。
    *   **导出按钮**: 导出 Excel 文件。

### 3.2 逻辑与规则
1.  **数据聚合**: 前端计算基础统计指标（未来数据量大时迁移至后端 SQL 聚合）。
2.  **全字段展示**: 相比移动端，Web端展示更完整的字段信息（如所有配套详情）。

### 3.3 开发计划
- [x] **UI 实现**: 完成管理后台布局与响应式表格。
- [x] **数据对接**: 完成全量数据读取。
- [ ] **搜索功能**: 实现基于小区名或地址的模糊搜索过滤。
- [ ] **导出功能**: 集成 `xlsx` 或 `SheetJS` 库，实现将当前表格数据导出为 Excel 文件。
- [ ] **高级筛选**: 增加按“总价范围”、“面积范围”筛选的功能。

---

## 4. 技术架构说明 (Technical Architecture)

*   **前端框架**: React 18 + Vite (Mobile First Design)。
*   **UI 系统**: Tailwind CSS (原子化样式) + Lucide React (图标库)。
*   **状态管理**: Zustand (轻量级全局状态管理)。
*   **后端服务**: Supabase (BaaS)。
    *   **Database**: PostgreSQL。
    *   **API**: RESTful API (Auto-generated)。
    *   **Security**: Row Level Security (RLS) 策略控制。

---

*文档生成时间: 2026-01-31*
