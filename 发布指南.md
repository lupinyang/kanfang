# 如何发布并在微信中使用

目前的“看房助手”是一个 **Web 应用 (H5)**。要在微信中使用它，最简单的方式是将它发布到互联网上，然后通过微信打开链接。

以下是两种主流的使用方案：

## 方案一：直接作为 H5 网页使用（推荐，最快）

不需要注册小程序账号，直接在微信里打开网页使用。

### 1. 部署上线
你需要将代码部署到支持静态网站托管的平台。
*   **Vercel / Netlify (推荐)**: 免费、速度快、自动支持 HTTPS。
    *   将代码提交到 GitHub。
    *   在 Vercel 导入项目，它会自动识别 `Vite` 框架。
    *   点击 Deploy，你将获得一个类似 `https://kanfang-app.vercel.app` 的域名。
*   **自有服务器**:
    *   在本地运行 `npm run build` 打包。
    *   将生成的 `dist` 目录上传到你的 Nginx/Apache 服务器。

### 2. 在微信中使用
*   将生成的 URL 发送给“文件传输助手”。
*   点击链接打开。
*   **设为浮窗** 或 **添加到收藏**，方便下次快速打开。

---

## 方案二：作为“微信小程序”发布（企业主体推荐）

如果你希望它像原生小程序一样出现在“最近使用的小程序”列表中，你可以创建一个“壳”小程序来加载这个 H5。**前提条件：你必须拥有企业主体的小程序账号，个人主体无法配置业务域名。**

以下是详细的操作清单，你可以按照步骤逐一完成并勾选：

### 阶段一：服务器与域名准备 (腾讯云)
- [ ] **1. 购买服务器**：在腾讯云购买一台云服务器（CVM 或 轻量应用服务器）。
- [ ] **2. 购买域名**：在腾讯云购买一个域名（例如 `kanfang.com`），并完成实名认证。
- [ ] **3. 域名备案**：在腾讯云备案系统提交 ICP 备案申请（需等待管局审核，约 7-20 个工作日）。
- [ ] **4. 域名解析**：备案通过后，在腾讯云 DNS 解析控制台，将域名（如 `m.kanfang.com`）解析到你的服务器公网 IP。
- [ ] **5. 申请 SSL 证书**：在腾讯云 SSL 证书控制台，为你的域名申请免费证书，并下载 Nginx 版本的证书文件。

### 阶段二：H5 项目打包与部署
- [ ] **1. 本地打包代码**：
    *   在开发工具的终端中运行命令：`npm run build`。
    *   打包完成后，项目根目录下会生成一个 `dist` 文件夹，这就是最终的网页静态文件。
- [ ] **2. 上传文件到服务器**：
    *   使用 FTP 工具（如 FileZilla、WinSCP）或腾讯云控制台的 WebShell 登录服务器。
    *   在服务器上创建一个目录，例如 `/www/wwwroot/kanfang`。
    *   将本地 `dist` 文件夹内的**所有内容**（不是 `dist` 文件夹本身，是里面的文件）上传到刚刚创建的目录中。
- [ ] **3. 配置 Nginx 服务器**：
    *   确保服务器已安装 Nginx。
    *   将下载的 SSL 证书文件上传到服务器（例如 `/etc/nginx/ssl/` 目录）。
    *   修改 Nginx 配置文件（通常在 `/etc/nginx/nginx.conf` 或 `/etc/nginx/conf.d/kanfang.conf`），参考以下配置：
    ```nginx
    server {
        listen 443 ssl;
        server_name m.kanfang.com; # 替换为你的域名

        # SSL 证书配置
        ssl_certificate /etc/nginx/ssl/your_cert.crt; # 替换为实际证书路径
        ssl_certificate_key /etc/nginx/ssl/your_cert.key; # 替换为实际私钥路径

        # 指向你上传的静态文件目录
        root /www/wwwroot/kanfang;
        index index.html;

        # 处理 React 路由，防止刷新 404
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
    ```
    *   重启 Nginx 服务：`sudo systemctl restart nginx` 或 `nginx -s reload`。
- [ ] **4. 验证部署**：在手机浏览器中输入 `https://你的域名`，检查网页是否能正常打开且显示安全锁（HTTPS）。

### 阶段三：微信小程序配置与发布
- [ ] **1. 注册小程序账号**：前往微信公众平台注册**企业主体**的小程序账号。
- [ ] **2. 配置业务域名**：
    *   登录小程序后台，进入【开发管理】->【开发设置】->【业务域名】。
    *   点击“添加”，输入你的域名（如 `https://m.kanfang.com`）。
    *   下载微信提供的【校验文件】。
    *   将校验文件上传到腾讯云服务器的网站根目录（即 `/www/wwwroot/kanfang`），确保通过 `https://你的域名/校验文件.txt` 可以访问。
    *   在微信后台点击“保存”。
- [ ] **3. 创建小程序壳项目**：
    *   下载并安装 **微信开发者工具**。
    *   新建项目，填入你的小程序 AppID，不使用云服务，选择“JavaScript - 基础模板”。
- [ ] **4. 编写 web-view 代码**：
    *   打开 `pages/index/index.wxml` 文件，清空原有代码。
    *   添加一行代码：`<web-view src="https://你的域名"></web-view>`。
- [ ] **5. 上传与提交审核**：
    *   在微信开发者工具中点击右上角的“上传”按钮。
    *   回到微信公众平台后台，在【版本管理】中找到刚上传的开发版本，提交审核。
    *   审核通过后，点击“发布”，你的小程序就可以被大家搜索和使用了。

### 调试技巧（绕过域名限制）
如果你没有备案的域名，或者只想自己用：
1.  在微信开发者工具中，点击详情 -> 本地设置 -> 勾选 **“不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书”**。
2.  点击“真机调试”，扫描二维码即可在手机上使用。

---

## 方案三：正式域名部署与备案流程（面向正式发布）

如果你希望小程序能正式发布上线（不仅仅是调试版），你需要完成严格的域名备案和HTTPS配置。

### 1. 域名准备与实名
*   **购买域名**：在阿里云/腾讯云购买一个域名（如 `kanfang.com`）。
*   **实名认证**：购买后需立即上传身份证进行实名认证（审核约1-3天）。

### 2. 服务器与 ICP 备案（强制）
微信要求小程序后端域名必须经过 **ICP备案**。
*   **购买服务器**：必须拥有一台**包月3个月以上**的国内服务器（阿里云/腾讯云）。
*   **提交备案**：在云厂商控制台提交备案申请（主体信息、网站信息、人脸核身）。
*   **时间成本**：管局审核通常需要 **7-20个工作日**。

### 3. SSL 证书配置（HTTPS）
微信强制要求 HTTPS 加密传输。
*   **申请证书**：在云厂商申请免费 SSL 证书（有效期通常为3个月或1年）。
*   **部署证书**：下载证书文件配置到你的 Nginx/Apache 服务器，开启 443 端口。

### 4. 关键限制（个人 vs 企业）
*   **个人主体小程序**：**不支持**配置 `web-view` 业务域名。这意味着**个人账号无法使用方案二的网页嵌套模式发布正式版**。
    *   *建议*：个人开发者请直接使用 **方案一 (H5链接)**，或者将项目重构为原生小程序代码。
*   **企业主体小程序**：支持配置业务域名，可完美使用方案二发布。

---

## 常见问题

### 1. 拍照无法使用？
微信 H5 支持直接调用系统相机。点击我们添加的“相机”图标，选择“拍照”即可。

### 2. 数据会丢失吗？
不会。因为我们已经对接了 Supabase 云数据库，无论你在哪里打开（电脑、手机、iPad），只要连上网，数据都是同步的。
